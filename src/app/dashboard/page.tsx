"use client";

import { Box, Typography } from "@mui/material";

type Props = Readonly<{}>;

export default function Page() {
  return (
    <Box sx={{ p: 2 }}>
      <Typography>Dashboard</Typography>
    </Box>
  );
}
